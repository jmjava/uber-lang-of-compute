# Lab queue for Volcano-scheduled domino chains (maps to blog "SyncSet" provisioning layer).
apiVersion: scheduling.volcano.sh/v1beta1
kind: Queue
metadata:
  name: kbl-lab
spec:
  weight: 1
  capability:
    cpu: "20"
    memory: 64Gi
  reclaimable: true
